import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-vue-tabs-sidemenu',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
